[project]
name = "inference"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "catboost",
    "scikit-learn",
    "pandas",
    "numpy",
    "pyarrow",
    "python-json-logger",
]
